- name: Install packages on all Linux servers
  hosts: linux
  become: yes

  tasks:
    - name: Ensure vim and nano are installed
      package:
        name:
          - vim
          - nano
        state: present
